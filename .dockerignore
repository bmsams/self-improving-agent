# Build artifacts
build/
dist/
*.egg-info/
*.egg

# Python cache
__pycache__/
__pycache__*
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
.venv/
.env
venv/
env/
ENV/

# Testing
.pytest_cache/
.coverage
.coverage*
htmlcov/
.tox/
*.cover
.hypothesis/
.mypy_cache/
.ruff_cache/

# Development
*.log
*.bak
*.swp
*.swo
*~
.DS_Store

# IDEs
.vscode/
.idea/

# Version control
.git/
.gitignore
.gitattributes

# Documentation
# Keep docs/ and markdown in the build context (benchmarks and the agent use them).

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml

# CDK output (can contain large/broken paths that break source zipping)
infra/cdk.out/
infra/cdk.out.agentcore/

# Agent runtime artifacts (must not be baked into the container image)
.agent_state.json
.benchmark_history.json
.junit-results.xml
reports/

# Bedrock AgentCore specific - keep config but exclude runtime files
.bedrock_agentcore.yaml
.dockerignore

# Keep wheelhouse for offline installations
# wheelhouse/
